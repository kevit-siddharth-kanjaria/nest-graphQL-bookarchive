type Author {
    id: ID!
    name: String!
    age: Int!
    languages: [String]
}

type Query {
    authors: [Author]
}

input AuthorDataInput {
    name: String!
    age: Int!
    languages: [String]
}

type Mutation {
    addAuthor(authorData: AuthorDataInput!): Author
}
